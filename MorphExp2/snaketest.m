N=64;phase = cumsum(ones(N,N)/32*2*pi);phasebend = ones(N,1) * cos((1:N)/32*pi);im = cos(phase+phasebend)+1;npts = 20;pts = zeros(npts,2);for i=1:npts	pts(i,1) = round(i/(npts+2)*N);	pts(i,2) = 30;end	pts(1,1) = 2;	pts(npts,1) = N-1;for i=1:8	[pts e]= grad_snake_y(pts, 0*ones(1,npts), .02*ones(1,npts), 4, 1, im, 0);	e	imagesc(im);	hold on	plot(pts(:,1),pts(:,2),'x');	hold off	drawnow;end